import { useState } from 'react'
export default function TeacherLogin(){
  const [email,setEmail] = useState('')
  const [pw,setPw] = useState('')
  async function login(){
    // We will add full auth later. For now this is a placeholder.
    alert('Teacher login not yet implemented. Continue to dashboard for demo.')
    window.location = '/teacher/dashboard'
  }
  return (<div style={{padding:20}}><h2>Teacher login</h2><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="email" /><input value={pw} onChange={e=>setPw(e.target.value)} placeholder="password" type="password" /><button onClick={login}>Login</button></div>)
}
